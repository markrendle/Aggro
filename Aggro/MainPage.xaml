<UserControl x:Class="Aggro.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400"
             KeyDown="UserControl_KeyDown"
             KeyUp="UserControl_KeyUp">

    <Canvas x:Name="LayoutRoot" Background="LightGray">
        <Rectangle Name="block" Width="32" Height="32"
                   Canvas.Top="{Binding Player.Location.X}"
                   Canvas.Left="{Binding Player.Location.Y}"
                   RenderTransform="{Binding Player.Transform}"
                   Fill="Red"
                   RenderTransformOrigin="0.5 0.5">
        </Rectangle>
    </Canvas>
</UserControl>
